const AntiCheatService=Object.freeze({record(c,p){p=requireObject_(p);const a=ownedAttempt_(c,p.attemptId);if(a.examStatus!==EXAM_STATUSES.IN_PROGRESS)throw AppError.conflict('การสอบสิ้นสุดแล้ว');const w={TAB_SWITCH:10,FULLSCREEN_EXIT:15,COPY:10,PASTE:10,MULTIPLE_TAB:30,DEVTOOLS_KEY:20},type=Utils.text(p.eventType).toUpperCase();if(!Object.prototype.hasOwnProperty.call(w,type))throw AppError.validation('เหตุการณ์ตรวจสอบไม่ถูกต้อง');const s=w[type],e=SecurityEventRepository.insert({attemptId:a.attemptId,eventType:type,severity:s>=30?'HIGH':s>=15?'MEDIUM':'LOW',riskScore:s,detailJson:{page:Utils.text(p.detailJson&&p.detailJson.page).slice(0,100),at:Utils.now()},occurredAt:Utils.now()},{actorId:c.userId});AttemptRepository.update(a.attemptId,{riskScore:Utils.num(a.riskScore)+s},{actorId:c.userId});return{recorded:true,eventType:type,riskScore:Utils.num(a.riskScore)+s}}});
