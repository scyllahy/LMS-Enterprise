const AntiCheatService=Object.freeze({record(c,p){const w={TAB_SWITCH:10,FULLSCREEN_EXIT:15,COPY:10,PASTE:10,MULTIPLE_TAB:30,DEVICE_CHANGE:40,DEVTOOLS_KEY:20},s=w[p.eventType]||5,e=SecurityEventRepository.insert({attemptId:p.attemptId,eventType:p.eventType,severity:s>=30?'HIGH':s>=15?'MEDIUM':'LOW',riskScore:s,detailJson:p.detailJson||{},occurredAt:Utils.now()},{actorId:c.userId}),a=AttemptRepository.findById(p.attemptId);if(a)AttemptRepository.update(a.attemptId,{riskScore:Utils.num(a.riskScore)+s},{actorId:c.userId});return e}});
