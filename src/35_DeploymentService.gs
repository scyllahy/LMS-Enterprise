const DeploymentService=Object.freeze({status(c){PermissionService.requireAny(c,['system.monitor','system.manage']);return{version:Props.get('APP_VERSION',APP_CONFIG.app.version),environment:APP_CONFIG.app.environment,maintenance:Utils.bool(Props.get('MAINTENANCE_MODE','false'))}},backupAndUpgrade(c,v){PermissionService.require(c,'system.manage');const b=BackupService.backupAll(c);Props.set('APP_VERSION',v);return{upgraded:true,version:v,backup:b}}});
