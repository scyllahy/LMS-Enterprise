const BootstrapService=Object.freeze({installOrUpgrade(){const s=getSchema_();Object.keys(s).forEach(n=>DB.sheet(n,s[n]));return{sheets:Object.keys(s)}},seed(c){const r=repo_('Settings','settingId','SET');[['SCHOOL_NAME','โรงเรียนสระแก้ว'],['AI_ENABLED',false],['LINE_ENABLED',false],['TELEGRAM_ENABLED',false]].forEach(x=>{if(!r.findOne({settingKey:x[0]}))r.insert({settingKey:x[0],settingValueJson:x[1],description:''},{actorId:c.userId})});return{seeded:true}}});
